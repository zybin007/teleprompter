# 提词器 PWA 版 · 使用说明

## 一、这是什么

把原来的「提词器 H5」升级成了 **PWA（渐进式 Web 应用）**，并发布成了在线链接。
手机上打开后可以「添加到主屏幕」，获得**桌面图标 + 全屏 + 离线可用**的类原生 App 体验。

## 二、线上链接

- **分享链接**：https://15bb664b4a5a4174a88c3c6200c6fef0.app.workbuddy.host/
- **二维码**：见同目录 `qrcode.png`，手机扫码直达

## 三、手机端如何「装到桌面当 App 用」

### Android / 鸿蒙（体验最佳）
1. 用浏览器（Chrome / Edge / 系统浏览器）打开上面的链接
2. 首次打开会提示「添加到主屏幕」，或点浏览器菜单（⋮）→「添加到主屏幕 / 安装应用」
3. 桌面出现「提词器」图标，点开即全屏运行、离线可用

### iOS（Safari）
1. 用 Safari 打开链接
2. 点底部「分享」按钮 → 向上滑 →「添加到主屏幕」
3. 桌面出现图标，点开全屏运行

## 四、PWA 相对普通 H5 的增强

| 能力 | 说明 |
|---|---|
| 桌面图标 | 像 App 一样一键打开 |
| 全屏运行 | 无浏览器地址栏，沉浸式 |
| 离线缓存 | Service Worker 缓存页面资源，断网也能开 |
| 免审核 | 不用注册、不用审核，直接分享即用 |
| 跨平台 | iOS / Android / 鸿蒙 全通用 |

## 五、文件清单（D:\AI-work\pwa\）

| 文件 | 作用 |
|---|---|
| `index.html` | 主程序（提词器完整功能，含 v3.2 Markdown + 闪屏修复 + 点击暂停/继续） |
| `manifest.json` | PWA 清单（名称/图标/全屏/主题色） |
| `sw.js` | Service Worker（离线缓存） |
| `icon.png` | 应用图标（512×512） |
| `qrcode.png` | 分享二维码 |

## 六、说明

- 本 PWA 是**网页**（HTTP 链接访问），不是微信小程序。小程序需用 WXML/WXSS/JS 单独开发并走微信审核发布，本页可作为其功能参考。
- 讲稿数据仍存本机浏览器（localStorage），离线可用、不上传。
